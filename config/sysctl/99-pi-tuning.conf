# Keep the Pi responsive under mixed media + web load.
vm.swappiness=10
vm.vfs_cache_pressure=50
vm.dirty_background_ratio=5
vm.dirty_ratio=20

# Crash loops should not be able to fill the SD card with core dumps.
kernel.core_pattern=/dev/null
