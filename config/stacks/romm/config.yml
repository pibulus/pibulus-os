emulatorjs:
  cache_limit: null
  controls: {}
  debug: false
  disable_auto_unload: false
  disable_batch_bootup: true   # load cores on demand, not all at startup
  netplay:
    enabled: false
    ice_servers: []
  settings:
    default:
      fps: show
      shader: crt-easymode.glslp
      vsync: disabled
      threads: enabled          # SharedArrayBuffer threading; COOP/COEP headers are set
      virtual_gamepad: enabled  # show touch controls on mobile/tablet
      autosave: enabled         # auto save state on exit, auto load on launch
    # RomM merges settings by actual EmulatorJS core name, so keep both
    # broad labels and concrete core names for heavy systems.
    n64:
      threads: enabled
      shader: none              # CRT shader tanks N64 framerate, disable it
    mupen64plus_next:
      threads: enabled
      shader: none
    parallel_n64:
      threads: enabled
      shader: none
    psx:
      threads: enabled
      shader: none
    pcsx_rearmed:
      threads: enabled
      shader: none
    mednafen_psx_hw:
      threads: enabled
      shader: none
    nds:
      threads: enabled
      shader: none
    melonds:
      threads: enabled
      shader: none
    desmume:
      threads: enabled
      shader: none
    desmume2015:
      threads: enabled
      shader: none
exclude:
  platforms: &id001
  - '@eaDir'
  - __MACOSX
  - $RECYCLE.BIN
  - .Trash-*
  - .stfolder
  - .Spotlight-V100
  - .fseventsd
  - .DocumentRevisions-V100
  - System Volume Information
  - ._*
  roms:
    multi_file:
      names: *id001
      parts:
        extensions:
        - db
        - ini
        - tmp
        - bak
        - lock
        - log
        - cache
        - crdownload
        names: &id002
        - .DS_Store
        - .localized
        - .Trashes
        - .stfolder
        - '@SynoResource'
        - gamelist.xml
        - ._*
    single_file:
      extensions:
      - db
      - ini
      - tmp
      - bak
      - lock
      - log
      - cache
      - crdownload
      names: *id002
filesystem:
  firmware_folder: bios
  roms_folder: roms
scan:
  export_gamelist: false
  media:
  - box2d
  - screenshot
  - manual
  priority:
    artwork:
    - igdb
    - moby
    - ss
    - ra
    - launchbox
    - gamelist
    - hasheous
    - tgdb
    - flashpoint
    - hltb
    language:
    - en
    - fr
    metadata:
    - igdb
    - moby
    - ss
    - ra
    - launchbox
    - gamelist
    - hasheous
    - tgdb
    - flashpoint
    - hltb
    region:
    - us
    - wor
    - ss
    - eu
    - jp
system:
  platforms:
    megadrive: genesis
  versions: {}
